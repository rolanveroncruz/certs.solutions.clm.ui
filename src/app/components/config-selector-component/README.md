# Config Selector Component
The ConfigSelectorComponent is the component making up the contents inside CertRequestModalComponent's Renewal Configuration Tab.
It is composed of two sections on either side:
- The Left Side contains a list of available configurations, and allows creating  a new one or cloning then editing an existing one.
- The Right Side contains the configuration details
- 
