# Config Form Component

The ConfigFormComponent composes the right side form of the ConfigSelectorComponent.
When a configuration is selected in the ConfigSelectorComponent, the ConfigFormComponent displays the configuration details.

The config form only shows specific fields:
- `name`
- `description`
- days before expiration
- check boxes for `notify on success` and `notify on failure`
- notification emails
